spring:
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 110MB
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    system:
      url: jdbc:mysql://127.0.0.1:3306/smart-system?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
      username: root
      password: ming8858*

logging:
  level:
    root: info
    com.smart: debug

management:
  endpoint:
    #    开启shutdown端点
    shutdown:
      access: none
    health:
      show-details: always
    logfile:
      access: unrestricted
    env:
      access: unrestricted
  endpoints:
    web:
      exposure:
        include: "*"
  health:
    mail:
      enabled: false

feign:
  sentinel:
    enabled: true